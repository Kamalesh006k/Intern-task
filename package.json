{
    "name": "primetrade-task-manager-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install:all": "npm install --prefix frontend-intern-task && pip install -r backend/requirements.txt",
        "build": "npm run build --prefix frontend-intern-task",
        "dev:frontend": "npm run dev --prefix frontend-intern-task",
        "dev:backend": "cd backend && uvicorn main:app --reload"
    }
}